# Standalone, non-production DR target.
DR_POSTGRES_USER=hope
DR_POSTGRES_PASSWORD=replace-with-a-local-secret
DR_POSTGRES_DB=hope_dr
DR_POSTGRES_PORT=55432

# Source database is never persisted in this file.
# Run the drill with SOURCE_DATABASE_URL set in the environment.
