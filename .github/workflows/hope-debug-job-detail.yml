name: HOPE Verify Current Job Detail

on:
  push:
    branches:
      - hope/full-stack-hardening-2026-09-17
    paths:
      - test-results/debug-job-detail-trigger.txt
  workflow_dispatch:

permissions:
  contents: write

env:
  BRANCH: hope/full-stack-hardening-2026-09-17

auto_cancel: false

jobs:
  verify-current:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout exact working branch
        uses: actions/checkout@v5
        with:
          ref: ${{ env.BRANCH }}
          fetch-depth: 0

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.47.2
          channel: stable
          cache: true

      - name: Flutter dependencies
        run: flutter pub get

      - name: Analyze current production source
        shell: bash
        run: |
          flutter analyze --no-fatal-warnings --no-fatal-infos

      - name: Run exact current job-detail regression test
        id: job_detail_test
        shell: bash
        run: |
          set +e
          flutter test --no-pub test/features/marketplace/job_detail_page_test.dart --reporter expanded >/tmp/job-detail-test.log 2>&1
          rc=$?
          {
            echo '# Current Job Detail Verification'
            echo
            echo "Branch: ${BRANCH}"
            echo "Test exit=${rc}"
            echo
            echo '## flutter test'
            echo '```text'
            cat /tmp/job-detail-test.log
            echo '```'
          } > test-results/job-detail-current-verification.md
          printf 'JOB_DETAIL_TEST_EXIT=%s\n' "$rc" >> test-results/job-detail-current-verification.md
          echo "test_exit=$rc" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Persist current verification checkpoint
        if: always()
        shell: bash
        run: |
          set -Eeuo pipefail
          git config user.name 'HOPE CI'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add test-results/job-detail-current-verification.md
          git commit -m '[CI-VERIFY] persist current job detail regression result' || true
          git push origin HEAD:${BRANCH}

      - name: Enforce exact test result
        if: always()
        shell: bash
        run: |
          rc='${{ steps.job_detail_test.outputs.test_exit }}'
          if [[ -z "$rc" ]]; then
            echo 'Job-detail test did not produce an exit code.'
            exit 1
          fi
          exit "$rc"
