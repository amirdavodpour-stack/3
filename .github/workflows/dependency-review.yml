name: HOPE Dependency Review

on:
  pull_request:
    branches: [Main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: hope-dependency-review-${{ github.event.pull_request.base.ref || github.ref }}
  cancel-in-progress: true

jobs:
  dependency-review:
    name: Dependency review
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Dependency review
        uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294 # v5.0.0
        with:
          fail-on-severity: high
